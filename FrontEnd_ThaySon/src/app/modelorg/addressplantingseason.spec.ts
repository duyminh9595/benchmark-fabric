import { Addressplantingseason } from './addressplantingseason';

describe('Addressplantingseason', () => {
  it('should create an instance', () => {
    expect(new Addressplantingseason()).toBeTruthy();
  });
});
